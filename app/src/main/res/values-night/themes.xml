<resources>
    <!-- Night theme — Compose handles dark mode via MaterialTheme, no separate XML needed -->
    <style name="Theme.ChibiClaw" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
